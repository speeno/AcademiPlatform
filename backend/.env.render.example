# Render 프로덕션 DB 연결 예시 (비밀번호는 대시보드에서 복사)
# PostgreSQL > Connections > Internal Database URL (Web Service용)
# PostgreSQL > Connections > External Database URL (로컬 migrate/seed용)

# Internal (academiq-backend Web Service 환경변수 DATABASE_URL)
DATABASE_URL=postgresql://academiqdb_09uu_user:YOUR_PASSWORD@dpg-d841p0eq1p3s738igfd0-a:5432/academiqdb_09uu?sslmode=require

# 로컬에서 아래 스크립트 실행 시에는 External URL 사용:
# ./scripts/render-db-provision.sh
